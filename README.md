# Margo

Automatically generate citations for news articles and academic papers.

## Build

```bash
bun run build
```

Load the generated `./dist` directory with `chrome://extensions` using `Load unpacked`.
